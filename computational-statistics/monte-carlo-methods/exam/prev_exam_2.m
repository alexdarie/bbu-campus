## Only 80% of the components for some electronic equipment pass a special ## inspection. Conduct a Monte Carlo study to estimate:#### a. the probability that at least 3 components fail the inspection before one passes it## b. the expected number of components that fail the inspection before one passes itp = 0.8;num_simul = compute_num_simul(0.005, 0.05);A = geo(num_simul, p);fprintf("Simulated prob. P(A >= 3) := %1.5f\nErr e := %e\n\n", mean(A >= 3), abs(mean(A >= 3) - (1 - geocdf(2, p))));fprintf("Mean E(A) := %1.5f\nErr e := %e\n\n", mean(A), abs(mean(A) - (1-p)/p));